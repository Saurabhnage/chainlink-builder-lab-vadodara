# Scripts

This folder contains the deployment and interaction scripts used during the workshop.

- `scripts/deploy.ts` chooses a track from the `TRACK` environment variable.
- `scripts/track3/send-ccip-message.ts` sends the cross-chain message after deployment.
